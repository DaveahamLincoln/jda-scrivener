<%= form_for(@flood) do |f| %>
  <% if @flood.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flood.errors.count, "error") %> prohibited this flood from being saved:</h2>

      <ul>
      <% @flood.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
